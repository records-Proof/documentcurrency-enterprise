export default function Registry() {
  return (
    <main className="min-h-screen bg-[#0B0E14] text-[#E6E6E6]">
      <div className="mx-auto max-w-4xl px-6 py-10">
        <a className="text-sm text-[#F2D27A]" href="/">← Home</a>
        <h1 className="mt-4 text-3xl font-semibold">Registry</h1>
        <p className="mt-2 text-sm text-[#A7B0C0]">
          MVP placeholder. Next we’ll load issued records from DB via /api/registry.
        </p>
      </div>
    </main>
  );
}
